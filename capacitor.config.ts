import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kgh07.catmouse.survivor',
  appName: 'Cat Mouse Survivor',
  webDir: 'dist',
  android: { allowMixedContent: false },
};

export default config;
